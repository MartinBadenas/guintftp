#include "../tftp_packet.c"

void test_buff_to_packet_read_write() {
	assert(1 == 2);
	//char packet[512+4];
	//int i = buff_to_packet_read_write(char *buff, int bufflen, struct packet_read_write *paquet);
	//assert(i == 0);
}